apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ci-test-stack.consoleName" . }}-config
  labels:
    {{- include "ci-test-stack.labels" . | nindent 4 }}
data:
  plugin-proxy.json: |
    {}
